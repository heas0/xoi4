{
  "name": "hexagonal-cells",
  "version": "1.0.0",
  "description": "Hexagonal grid system over block map in 3D",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:countries": "python scripts/generate_country_assignments.py",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist --no-history"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}